// import * as pc from 'playcanvas';

// export function createAim(cameraEntity, assets) { 
//     const aim = new pc.Entity('Aim');    
    
//     // Thêm thành phần 'element' để sử dụng hình ảnh PNG
//     aim.addComponent('element', {
//         type: 'image',
//         anchor: new pc.Vec4(0.5, 0.5, 0.5, 0.5),  // Đặt anchor ở giữa màn hình
//         width: 64,  // Đặt chiều rộng của crosshair
//         height: 64, // Đặt chiều cao của crosshair
//         texture: assets.aim.texture // Sử dụng texture từ assets
//     });
    
    
//     aim.setLocalPosition(0, 0, 0);

    
//     return aim; 
// }
